let index = 0
while (index <=10) {
    console.log(`value of index is ${index}`);
    index = index + 2;
}

// ++++++

let myArray = ['superman', 'spiderman', 'heman', 'flash', 'batman'];
let arr = 0
while(arr < myArray.length) {
    console.log(`Value is ${myArray[arr]}`);
    arr++;
}

// ++++++++

let score = 0
do {
    console.log(`Score is ${score}`);
    score++
} while (score<=10);